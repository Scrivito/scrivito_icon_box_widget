<% IconBoxWidget.sizes.each do |size| %>
  <%= cms_tag(:button, widget, attribute, class: "btn button-picker #{widget.send(attribute) == size ? "selected" : ""}", :"data-picker-content" => size, :"data-editor" => 'button-picker') do %>
    <%= size %>
  <% end %>
<% end %>
