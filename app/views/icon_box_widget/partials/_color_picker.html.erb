<%= scrivito_toggle_button_editor(widget, attribute, IconBoxWidget.colors) do |elem|
  scrivito_tag(:button, widget, attribute, style: "background-color: #{elem}", class: elem == widget.send(attribute) ? 'active' : '', data: {editor: 'scrivito-toggle-button', content: elem}) do
    elem
  end
end %>