<div class="button-picker-wraper">
  <% IconBoxWidget.colors.each do |color| %>
    <%= cms_tag(:button, widget, attribute, class: "btn web_button button-picker #{widget.send(attribute) == color ? "selected" : ""}",  style: "background: #{color}") do %><%= color %><% end %>
  <% end %>
</div>